---
description: 在 require-ana 目录整理需求并生成会议三件套产物
globs: require-ana/**
alwaysApply: false
---

# require-ana 需求整理

处理 `require-ana/` 时，**必须先读** `require-ana/AI-GUIDE.md`。

## 核心约束

- 用户输入**可随意**（口语、碎片、聊天记录）；AI 负责整理、去重、**叠加**到 `registry.yaml` 再出产物。
- 每次投喂：**先读写完台账**，再按需生成 01/02/03。
- 三份产物：`01_会前确认清单.md`、`02_现场确认表.md`、`03_会后结论.md`。
- 状态顺序：**待确认 → 执行中 → 待办**；编号 `REQ-001` 起，稳定不随改标题变。
- **不要**负责人/owner 字段或列。
- **待确认**表头：`编号 | 标题 | 说明 | 待商榷`（待商榷列填 open_questions）；**待办**「依赖或建议排期」**留空**（—）。
- 禁止编造信息。
- 输出：`require-ana/meeting/{YYYY-MM-DD}/`；模板见 `require-ana/templates/`。

## 指令

`出会前清单` → 01 | `出确认表` → 02 | `出会后结论` → 03 | `三份一起出` → 全部

回复：新增/叠加/合并条数 + 路径；不贴全文。
