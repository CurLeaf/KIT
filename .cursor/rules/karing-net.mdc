---
description: Karing / proxy / extension-host 网络只走 karing-net
alwaysApply: true
---

# karing-net

本机 Karing 控制面是 `D:\KIT\karing-net\karing-net.ps1`。

允许：`karing-net.ps1 status`（可读 `-Api`）。

禁止（除非用户当句点名 `sync` / `restart`）：

- 运行 `apply_recommended_net.ps1`、`manual_to_auto.ps1`
- `curl` / `Invoke-WebRequest` 走 `127.0.0.1:3067` 或访问 `api2.cursor.sh` 当验收
- `-FlushConnections`、`DELETE /connections`（无 id）、任何「timeout recovery」flush
- 重启 `karing.exe` / `karingService.exe`，或 `-ForceRestart`

探活只看本机进程和 `127.0.0.1:3057` / `3067` 是否在听。锁冲突退出码 3：停下，不要重试、不要再开一套脚本。
